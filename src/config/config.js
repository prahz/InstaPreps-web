// src/config/config.js

// const getApiUrl = () => {
//   if (process.env.NODE_ENV === 'development') {
//     // Use the staging API in development mode
//     return process.env.NEXT_PUBLIC_API_URL_STAGING;
//   } else if (process.env.NODE_ENV === 'production') {
//     // Use the production API in production mode
//     return process.env.NEXT_PUBLIC_API_URL_PRODUCTION;
//   } else {
//     // Fallback in case NODE_ENV is not set correctly
//     console.error("NODE_ENV is not set correctly. Defaulting to staging API.");
//     return process.env.NEXT_PUBLIC_API_URL_STAGING;
//   }
// };

// export default getApiUrl;


